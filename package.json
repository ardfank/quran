{
  "name": "quran",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "sudo php -S localhost:80 & wrangler dev --env=local"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
